# FIX-292: develop already reuses test workers; qualify the same suite with eight workers.
# Only demonstrated exceptions belong here. This non-matching sentinel keeps an empty serial execution empty.
%regex[^$]
